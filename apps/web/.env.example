# Supabase — copiar valores desde Settings → API del dashboard
# Project URL (ej: https://abcdefgh.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=

# Anon key (la que dice "anon", NO la service_role)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Telegram Login Widget — nombre del bot (ej: NuestrosGastosBot)
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=

# SECRETO: Token del bot de Telegram (para verificar hash server-side)
# ⚠️ Configurar en Vercel Dashboard → Settings → Environment Variables
TELEGRAM_BOT_TOKEN=

# SECRETO: Service Role Key de Supabase (bypasses RLS, solo para API routes)
# Settings → API → service_role secret (NO la anon key)
SUPABASE_SERVICE_KEY=
